[project]
name = "resources"
version = "0.1.0"
description = "Leashline configuration resources"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/resources"]
